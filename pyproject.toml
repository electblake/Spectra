[project]
name = "spectra"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.10"
dependencies = [
    "numpy>=1.24.0",
    "pillow>=10.0.0",
    "scikit-learn>=1.3.0",
    "scipy>=1.10.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0.0",
    "ruff>=0.16.1",
]
